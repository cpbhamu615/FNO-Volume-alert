F&O Stocks Volume Scanner

1. Install required libraries:
   pip install -r requirements.txt

2. Run the app:
   python volume_scanner_gui.py

3. Make sure beep.mp3 is in the same folder for sound alert.